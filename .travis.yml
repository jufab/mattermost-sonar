language: node_js
node_js:
  - "6"
dist: trusty
sudo: required

jdk:
  - oraclejdk8

addons:
  sonarqube:
    token : $SONAR_TOKEN

before_script:
  - npm install -g gulp

script:
  - gulp test

after_success:
  - sonar-scanner
